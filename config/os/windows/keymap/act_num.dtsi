        win_act_num_layer {
            /* WIN_AXN -- conditional on <WIN AXN>.
             *
             * Overrides the AXN positions whose macOS chord differs on
             * Windows. Everything else is &trans and falls through to AXN.
             *
             * Left shortcuts: Cmd+letter on Mac becomes Ctrl+letter here.
             * Row 0: Ctrl+W/E/R/T  (close, open, reload, new tab)
             * Row 1: Ctrl+A/S/D/F  (select-all, save, bookmark, find)
             *        Ctrl+H at G position for replace (macOS uses Opt+Cmd+F)
             * Row 2: Ctrl+Z/X/C/Y + paste (clipboard, same as Mac but native)
             *
             * Without this, AXN's Win+Z/X/C/V opens snap layouts, the
             * power-user menu, Copilot and clipboard history instead.
             *
             * Numpad home-row mods at 21/22 are swapped here: AXN has
             * RCTRL+N6 and RGUI+EQUAL; Windows swaps them to RGUI+N6,
             * RCTRL+EQUAL so the primary modifier stays on the same finger. */
            #define WA_L     &hmr  RGUI   N6
            #define WA_SEMI  &hmr  RCTRL  EQUAL

            display-name = "WIN_AXN";
            bindings = <
    &trans  &trans  &kp LC(W)  &kp LC(E)  &kp LC(R)      &kp LC(T)        &trans  &trans  &trans   &trans   &trans  &trans
    &trans  &kp LC(A)  &kp LC(S)  &kp LC(D)  &kp LC(F)  &kp LC(H)        &trans  &trans  &trans    WA_L   WA_SEMI  &trans
    &trans  &kp LC(Z)  &kp LC(X)  &kp LC(C)  &mp_wa_paste  &kp LC(Y)      &trans  &trans  &trans   &trans   &trans  &trans
                            &trans  &trans  &trans        &trans  &trans  &trans
            >;
        };
